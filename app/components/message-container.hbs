<main {{did-insert this.loadMessages}} {{did-update this.loadMessages @channel}}
	class="w-full p-2 bg-gray-200 h-screen main flex flex-col justify-between">
	{{yield this.messages (hash
	createMessage=this.createMessage
	deleteMessage=this.deleteMessage
	)}}
</main>